---
name: slide-storyboard
description: 既存のアジェンダを使うか、必要な情報からアジェンダを作成し、全スライドの構成案をまとめて確認・修正してHTMLのストーリーボードとして書き出す。
disable-model-invocation: true
---

# スライド・ストーリーボード作成

パワーポイント資料を作る前段階として、全スライドの構成案を先に作り、デッキ全体を見ながら調整する。決まった構成は `assets/template.html` をもとにしたHTMLドキュメントとして書き出す。ユーザーはそのHTMLを見ながらパワーポイントを作成する。

## 進め方の原則

最初に確認するのは、資料全体の構成を決めるうえで不足している重要な情報だけにする。ユーザーがすでに伝えたことは聞き直さない。情報が揃ったら、各スライドを個別に確定する前に全スライドのドラフトを提示する。ユーザーの修正はデッキ全体に反映し、追加質問は判断に必要な未解決事項へ絞る。

## 手順

### 1. アジェンダを用意する

アジェンダがあるか確認し、提示済みならそのまま使う。

- **既にある場合** — ファイルパス、または直接貼り付けたアウトラインを受け取る。ファイルが指定されたら読み込む。スライド単位に分かれていなければ、内容に合わせて区切りを提案する。
- **ない場合** — 資料の目的、対象者、最も伝えたいこと、扱う話題、発表時間や枚数の制約から、構成に必要な不足情報だけを確認する。回答をもとに、順序とスライドの粗い区切りを含むアジェンダ案を作る。

既存アジェンダでも新規アジェンダでも、この段階で各項目の承認を待たずに手順2の全スライドのドラフトへ進む。ただし、目的や対象者などが曖昧で構成が大きく変わる場合は、その点を先に尋ねる。

### 2. 全スライドのドラフトをまとめて提示する

アジェンダをもとに、デッキ全体の順序が分かる一覧を作る。各スライドに次の案を含める。表紙・扉などはキーメッセージを省いてよい。

- 番号、タイトル、役割と前後のつながり
- キーメッセージの下書き(必要なスライドは100〜150字程度)
- 内容に適した視覚要素(イラスト / 図 / 表・グラフ / なし)と、具体的な構図または表・グラフの概略
- 下記プリセットから選んだレイアウト案と、その配置が内容に合う理由

全体案を提示する前に、流れに抜けがないか、主張や例の重複がないか、時間・枚数の制約に収まるかを確認する。ユーザーにはデッキ全体の流れと直したい箇所を見てもらう。各スライドのタイトル・視覚要素・配置を機械的に一問ずつ尋ねない。

### 3. 全体案を修正し、制作に必要な詳細を整える

ユーザーの修正を該当スライドと関連するスライドへ反映する。修正後はデッキ全体を見渡し、順序、重複、欠落、前後のつながり、キーメッセージと視覚表現の整合を再確認する。変更箇所が分かる形で更新した全体案を示す。再検討は影響する箇所に絞り、他のスライドを毎回作り直さない。

判断が分かれる重要事項だけを質問する。レイアウトや視覚表現は内容から具体案を提案し、ユーザーに全項目の個別承認を求めない。

イラストや図を使うスライドには、具体的な英語の画像生成プロンプトを作る。共通スタイルの指定が既にあれば各プロンプトの末尾に付ける。指定がなければデッキに合う共通スタイルを提案し、必要な場合にだけ確認する。表・グラフにはプロンプトの代わりに必要な行・列・軸の構成を書く。

### 4. レイアウト・プリセット

提案時は次から選ぶ。どれにも当てはまらない場合はその場で新しいプリセット名を作ってよい。

| プリセット名 | 構成 |
|---|---|
| `left-text-right-visual` | 左にテキスト、右に視覚要素 |
| `right-text-left-visual` | 右にテキスト、左に視覚要素 |
| `top-visual-bottom-text` | 上に視覚要素、下にテキスト |
| `center-visual-split-text` | 中央に視覚要素、左右に箇条書き |
| `full-bleed-visual` | 視覚要素を全面に敷き、テキストを重ねる |
| `text-only` | 視覚要素なし |
| `numbered-agenda-path` | 番号付きの節目を一本の道筋として配置 |
| `statement-with-evidence` | 大きな結論と、下段の根拠3点 |
| `kpi-strip` | 見出しの下にKPIカードを横一列で配置 |
| `before-after-rows` | 左右を同じ行構造で対応付ける比較 |
| `horizontal-milestone-timeline` | 横軸に時点とマイルストーンを配置 |
| `vertical-milestone-timeline` | 縦の時間軸と右側の詳細説明 |
| `chevron-process-flow` | 連結した矢印で工程の進行を表現 |
| `roadmap-lanes` | 複数チーム・機能の期間を横レーンで表示 |
| `hierarchy-tree` | 上位概念から下位要素へ広がる階層図 |
| `logic-tree` | 問い・結論から論点や要因へ分岐 |
| `venn-overlap` | 複数の集合と共通領域を表示 |
| `radial-facets` | 中心概念から周囲の観点へ放射状に展開 |
| `chart-with-takeaway` | 結論見出し、チャート、注釈の順に配置 |
| `magazine-sidebar` | 主本文と引用・補足のサイドバー |
| `myth-fact` | よくある誤解と正しい対応の対比 |
| `procedure-checklist` | 準備、標準手順、完了条件・記録の3列 |
| `glossary-callout` | 用語、定義、現場例・注意点のカード |
| `workshop-exercise` | 個人検討、グループ討議、共有の3段階 |
| `training-objectives-agenda` | 研修目的、到達基準、当日の流れ |
| `training-roadmap` | 説明、デモ、演習、現場適用の流れ |
| `demonstration-walkthrough` | デモ、実施手順、完成例の3列 |
| `precheck-current-state` | 受講前の現状、課題、期待の3列 |
| `action-plan` | 学び、変える行動、期限・確認の3列 |

各プリセットの `.zone` 構成(何個の text/visual ボックスをどう並べるか)は `assets/template.html` 内のコメント例を参照する。ワイヤーフレームの各表示領域(カードや図表領域も含む)に Z1、Z2…の番号を振り、完成文言欄と素材仕様欄に同じ番号を使う。スライドタイトルは「共通タイトル」として記載する。図・写真・グラフのゾーンに文字を重ねる場合は、その文字も完成文言欄に含める。

### 5. HTMLに書き出す

全体案への修正を反映し、制作に必要な詳細が揃ったら `assets/template.html` を読み込み、カードブロックをスライドの枚数だけ複製して埋める。埋める項目は次の通り。

- スライド番号
- タイトル
- キーメッセージ(構成上の要約。スライドに表示するなら完成文言にも記載)
- レイアウト・プリセット名(ワイヤーフレームの表示に使う)
- ゾーン番号と、それぞれに配置する完成文言。見出し・本文・箇条書き・図中ラベル・表示する注釈を省略しない
- 視覚素材の仕様(構図、英語の画像生成プロンプト、図の要素と関係など)。テキスト中心のスライドでは「視覚素材なし」と記す
- 表・グラフの仕様(種類、行・列または軸・系列、値、単位、対象期間、注釈、出典)。グラフは全プロットに必要な値を列挙し、表は全セルを埋める。視覚的な概念図だけなら数値欄は不要
- 発表者向け補足、出典・根拠、素材の準備事項、未確定事項を別欄に分ける。出典は資料名・作成者・日付・URLやページ等、後でたどれる情報を記す。根拠がないものを確定情報として書かない

テキスト中心のスライドは各段落・箇条書きを完成文言欄に書く。図中心のスライドは図中ラベルと要素の関係を完成文言欄・視覚素材の仕様に分ける。表・グラフ中心のスライドは表示する見出し・軸名・凡例・注釈を完成文言欄に書き、データ表には必要な全値・単位・出典を記す。

出力前に全カードを見直し、①ワイヤーフレームの各ゾーンと完成文言・素材仕様が対応しているか、②表示予定の文字が完成文言欄から漏れていないか、③図表の項目・値・単位・注釈・出典が揃っているか、④要確認の値や出典が事実として表示されていないかを確認する。「要確認」が残る場合は該当箇所をHTMLにも明示し、確認事項を未確定事項欄にまとめる。

保存先の指定がなければ、アジェンダファイルを読み込んだ場合はそのファイルと同じディレクトリに、それ以外の場合は現在の作業ディレクトリに `slide-storyboard.html` として保存する。

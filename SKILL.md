---
name: slide-storyboard
description: アジェンダから対話形式でスライド構成(タイトル・キーメッセージ・レイアウト・イラスト構図・画像生成プロンプト)を1枚ずつ詰め、HTMLのストーリーボードとして書き出す。
disable-model-invocation: true
---

# スライド・ストーリーボード作成

パワーポイント資料を作る前段階として、スライド1枚ごとの構成を対話で固め、`assets/template.html` をもとにしたHTMLドキュメントとして書き出す。ユーザーはそのHTMLを見ながらパワーポイントを作成する。

## 進め方の原則

質問は一問ずつ出す。複数の論点をまとめて聞かない。ユーザーの回答を待ってから次の質問に進む。1枚のスライドの構成(手順4のa〜g)がすべて確定してから、次のスライドに進む。

## 手順

### 1. アジェンダの入力を受け取る

アジェンダのファイルパス、または直接貼り付けたアウトラインを尋ねる。ファイルが指定されたら読み込む。アジェンダがまだスライド単位に分かれていない場合は、スライドの粗い区切り案を提示し、ユーザーに確認する。

### 2. デッキ共通のイラストスタイルを確認する

このデッキで使う画像生成プロンプトの共通スタイル指定(各プロンプトの末尾に付ける文字列)を尋ねる。ユーザーが以前使ったスタイルを再利用したい場合はそれを使う。一度決めたら、以降すべてのスライドのプロンプトに同じ文字列を付ける。

### 3. スライドごとに、次の順で1問ずつ詰める

この順序を守り、a〜g がすべて確定するまで次のスライドに進まない。

- **a. タイトル** — アジェンダの項目から提案し、確認する。表紙・扉ページなど見出しだけで完結するスライドだと判断した場合は、その旨を伝えて b をスキップしてよいか先に確認する。
- **b. キーメッセージ** — (スキップしなかった場合)100〜150字程度で下書きし、ユーザーの反応を見て言葉を詰める。下書きを作る前に省略の要否を確認しているため、ここでは下書きをいきなり提示してよい。
- **c. 視覚要素の要否** — イラスト / 図 / 表・グラフ / なし のどれかを尋ねる。
- **d. 構図**(視覚要素ありの場合)— 具体案を1つ、選んだ理由とトレードオフとともに提案し、確認する。複数案を並べて選ばせない。
- **e. スライド上の配置**(視覚要素ありの場合)— 下記のレイアウト・プリセットから1つ提案し、確認する。
- **f. 画像生成プロンプト**(イラスト/図の場合)— 英語で具体的に書き、手順2で決めた共通スタイル文字列を末尾に付ける。
- **g. 表・グラフの構成**(表・グラフの場合)— 画像生成プロンプトの代わりに、必要な行・列・軸の構成をテキストで書く。

### 4. レイアウト・プリセット

提案時は次から選ぶ。どれにも当てはまらない場合はその場で新しいプリセット名を作ってよい。

| プリセット名 | 構成 |
|---|---|
| `left-text-right-visual` | 左にテキスト、右に視覚要素 |
| `right-text-left-visual` | 右にテキスト、左に視覚要素 |
| `top-visual-bottom-text` | 上に視覚要素、下にテキスト |
| `center-visual-split-text` | 中央に視覚要素、左右に箇条書き |
| `full-bleed-visual` | 視覚要素を全面に敷き、テキストを重ねる |
| `text-only` | 視覚要素なし |
| `numbered-agenda-path` | 番号付きの節目を一本の道筋として配置 |
| `statement-with-evidence` | 大きな結論と、下段の根拠3点 |
| `kpi-strip` | 見出しの下にKPIカードを横一列で配置 |
| `before-after-rows` | 左右を同じ行構造で対応付ける比較 |
| `horizontal-milestone-timeline` | 横軸に時点とマイルストーンを配置 |
| `vertical-milestone-timeline` | 縦の時間軸と右側の詳細説明 |
| `chevron-process-flow` | 連結した矢印で工程の進行を表現 |
| `roadmap-lanes` | 複数チーム・機能の期間を横レーンで表示 |
| `hierarchy-tree` | 上位概念から下位要素へ広がる階層図 |
| `logic-tree` | 問い・結論から論点や要因へ分岐 |
| `venn-overlap` | 複数の集合と共通領域を表示 |
| `radial-facets` | 中心概念から周囲の観点へ放射状に展開 |
| `chart-with-takeaway` | 結論見出し、チャート、注釈の順に配置 |
| `magazine-sidebar` | 主本文と引用・補足のサイドバー |
| `myth-fact` | よくある誤解と正しい対応の対比 |
| `procedure-checklist` | 準備、標準手順、完了条件・記録の3列 |
| `glossary-callout` | 用語、定義、現場例・注意点のカード |
| `workshop-exercise` | 個人検討、グループ討議、共有の3段階 |
| `training-objectives-agenda` | 研修目的、到達基準、当日の流れ |
| `training-roadmap` | 説明、デモ、演習、現場適用の流れ |
| `demonstration-walkthrough` | デモ、実施手順、完成例の3列 |
| `precheck-current-state` | 受講前の現状、課題、期待の3列 |
| `action-plan` | 学び、変える行動、期限・確認の3列 |

各プリセットの `.zone` 構成(何個の text/visual ボックスをどう並べるか)は `assets/template.html` 内のコメント例を参照する。

### 5. HTMLに書き出す

全スライド分の情報が揃ったら `assets/template.html` を読み込み、カードブロックをスライドの枚数だけ複製して埋める。埋める項目は次の通り。

- スライド番号
- タイトル
- キーメッセージ
- レイアウト・プリセット名(ワイヤーフレームの表示に使う)
- 構図の説明(視覚要素ありの場合)
- 画像生成プロンプト、または表・グラフの構成
- 備考(伝えたいことのメモ、決まっていない事項)

保存先をユーザーに尋ねる。指定がなければ、アジェンダファイルと同じディレクトリに `slide-storyboard.html` として保存する。
